{
  "name": "backoffice-adventures",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "БекОфисные приключения — юмористическая обучающая текстовая игра для IT-команд",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.8"
  }
}
